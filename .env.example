# Générer un hash : bun -e "import('./src/lib/auth.ts').then(m=>console.log(m.hashPassword('m-dp')))"
EDIT_PASSWORD_HASH=salt:hash

# Token GitHub avec accès repo (commence par ghp_)
GITHUB_TOKEN=

# Format : owner/repo
GITHUB_REPO=
